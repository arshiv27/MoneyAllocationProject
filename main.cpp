/*This file creates instance of the MoneyAllocator class and calls the run method to start the program.
It includes the MoneyAllocator.h header file. */

#include "MoneyAllocator.h" //declares the MoneyAllocator class

//starting point of the program
int main() {
    MoneyAllocator moneyAllocator; //creates an instance of the MoneyAllocator class
    moneyAllocator.run();
    return 0; 
}